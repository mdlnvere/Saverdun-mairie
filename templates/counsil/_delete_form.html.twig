<form method="post" action="{{ path('app_counsil_delete', {'id': counsil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ counsil.id) }}">
    <button class="btn">Delete</button>
</form>
